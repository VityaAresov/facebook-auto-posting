version: "3.9"
services:
  autoposter-bridge:
    build: .
    container_name: autoposter-bridge
    ports:
      - "3721:3721"
    volumes:
      - ./bridge.config.json:/app/bridge.config.json:ro
      - ./bridge.data.json:/app/bridge.data.json
    restart: unless-stopped
